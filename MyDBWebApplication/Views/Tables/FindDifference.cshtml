@model MyDBWebApplication.Models.Table

@{
    ViewData["Title"] = "Find table differences";
}

<h1>Find table differences</h1>

<h4>Table</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="FindDifference">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="DatabaseId" class="control-label"></label>
                <select asp-for="DatabaseId" class="form-control" asp-items="ViewBag.DatabaseId"></select>
            </div>
            <div class="form-group">
                <label asp-for="DatabaseId" class="control-label"></label>
                <select asp-for="DatabaseId" class="form-control" asp-items="ViewBag.DatabaseId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="FindDifference" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    }

